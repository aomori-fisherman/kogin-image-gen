# こぎんトレース台エディタ T0 — 実使用フィードバック / バグ調査

> 記録日: 2026-07-23 ／ 対象: `editor.html`（T0・仮値版）
> フィードバック元: 伎海（2026-07-23・原文の趣旨を保って整理）
> 調査担当: dev

## ⚠ 対応方針（先に読む）

**T0.5 の UI改修は「池田さん（現場実務者）のフィードバック収集後」にまとめて実施する。**
理由＝手戻り防止。伎海の実使用フィードバックと池田さんの現場フィードバックを合流させ、
レイアウト刷新・パレット実測（G-2）・渡り分け（調査A）を1回の改修でまとめて設計・実装する。

- このファイルは「収集・分類・調査」まで。**個別の UI改修は今は実装しない。**
- 例外＝調査B（連続ペースト/一括塗り）が万一「真バグ」だった場合のみ即修正可の指示だったが、
  調査の結果 **真バグではなく導線問題**と判定した（後述）。よって**ソース変更はしていない**。
- 調査A（渡り分け）はデータモデル制約のため、**設計メモのみ**（実装は T0.5/T1）。

---

## 1. フィードバック分類サマリー（11項目）

| # | 内容（要約） | 分類 | 行き先 |
|---|---|---|---|
| 1 | メニューが左右展開で作業しづらい。Word風にフルメニューは上部（格納可）・常用はグリッド左（最小化可）・グリッド図を最大化 | T0.5 UI改修 | レイアウト刷新 |
| 2 | 下絵移動時に大きさをマス目指定したい（例: この下絵を横80目に合わせる） | T0.5 UI改修 | 下絵スケールのマス目指定 |
| 3 | ペンで3マス→隣接4マスで塗ると 3+4 でなく 7 に統合。渡りを意図的に分けたい | **バグ調査A** | データモデル拡張（設計メモ） |
| 4 | 糸・布の色が少ない。実こぎんの色を追加・選択したい | T0.5（データ）＋確認 | G-2実測パレット・池田さん確認 |
| 5 | 取り消し/やり直しをツールバー上部に配置 | T0.5 UI改修 | ツールバー再配置 |
| 6 | Google検索画像をそのまま取り込めるのが良い | ポジティブ | 維持（下絵D&D/ファイル選択） |
| 7 | 抽出済みモドコ221件をエディタに挿入したい | T1前倒し要望 | モドコスタンプ（T1） |
| 8 | 下絵を載せて自動で塗る機能 | T1前倒し要望 | 自動グリッド化（T1） |
| 9 | 消しゴムが行単位。全行消せる方が使い勝手いい | T0.5 UI改修/小機能 | 自由方向消し・クリア導線 |
| 10 | 選択範囲の操作も左ツールバーへ | T0.5 UI改修 | 操作パネル再配置 |
| 11 | 選択しても連続ペースト・一括塗りが使えない気がする | **バグ調査B** | 導線問題（真バグではない）→T0.5 |

---

## 2. 各項目の詳細と対応方針メモ

### #1 メニューのレイアウト（左右展開が作業しづらい）— T0.5 UI改修
- 現状: 3カラム固定（左=ツール/パレット、中央=キャンバス、右=グリッド設定/下絵/範囲選択操作/検証/保存）。パネルが左右に散り、グリッド図の表示面積が狭い。
- 要望: ①フルメニュー＝画面上部（Word風・格納可能）②常用項目＝グリッド左側（最小化可能）③グリッド図を最大化。
- 方針メモ: 上部リボン（ファイル/表示/検証など低頻度）＋左サイド（ツール/パレット/取消の高頻度）＋右サイドは折りたたみ既定。中央キャンバスを可変幅で最大化。**池田さんFB合流後にワイヤーを固める。**

### #2 下絵の大きさをマス目指定 — T0.5 UI改修
- 現状: 下絵移動ツール(6)でホイール拡縮のみ（`onWheel` が `underlay.scale` を ×1.1）。数値指定・マス目基準の指定は無い。
- 方針メモ: 「下絵を横 N 目に合わせる」入力を追加。`scale = (N × cellW) / underlay.w` で一発フィット。縦指定・アスペクト維持のON/OFFも検討。データモデル変更不要（`underlay.scale` を計算して代入するだけ）＝T0.5で安価。

### #3 渡りが統合される（3+4→7）— バグ調査A（→第3章）
- 現状の制約であることを確認済み。**現データモデルでは「同色隣接ランの意図的な区切り」は表現不可能**。設計メモは第3章。

### #4 糸・布の色が少ない — T0.5（データ）＋池田さん確認＋G-2実測
- 現状: `js/trace-config.js` の `PALETTE`（仮6色）・`FABRICS`（仮2色）はすべて「仮」。hex/名称/糸番号は G-2実測で差し替え予定。
- 方針メモ: **G-2実測パレット課題に接続。** 実こぎんで使われる色（津軽こぎんの定番糸色・地布の紺/生成/その他）を池田さんに確認し、`PALETTE`/`FABRICS` を実データで拡充。**設定1ファイル差し替えで完結**（trace-config.js のみ）＝データが揃えば実装は軽い。糸番号（在庫実糸との対応）も同時に確定したい。

### #5 取り消し/やり直しの配置 — T0.5 UI改修
- 現状: 左パネルのツール直下に `↶取消 / ↷やり直し`（`btn-undo`/`btn-redo`）。Ctrl+Z/Ctrl+Y も有効。
- 方針メモ: #1のツールバー刷新に合わせ、上部ツールバーへ移設。機能は既に動作（undo深さ50）＝配置のみの問題。

### #6 Google画像の取り込み — ポジティブ（維持）
- 該当実装: 下絵のドラッグ&ドロップ（`underlay-dz`）＋ファイル選択（`underlay-file`）＋サンプル。画像→canvas化して下絵レイヤに敷く導線が効いている。
- 方針メモ: この導線は壊さない。#1のレイアウト刷新でも下絵投入の手数は増やさない。

### #7 抽出済みモドコ221件の挿入 — T1前倒し要望
- 現状: T0はモドコスタンプ非搭載（DEVGUIDE明記＝T1機能）。
- 方針メモ: 221件の抽出モドコをスタンプとしてグリッドに挿入する機能はT1スコープ。前倒し可否は伎海判断。挿入時は本調査A（渡り分け）のデータモデルとの整合が前提になる（モドコ内の同色隣接ランを正しく保持できるか）。

### #8 下絵から自動で塗る — T1前倒し要望
- 現状: T0は自動グリッド化非搭載（DEVGUIDE明記＝T1機能）。既存 `index.html` 側の画像→こぎん変換MVP（quantize/kogin-render）に近い処理を、トレース台の下絵→cells自動生成として移植する構想。
- 方針メモ: T1スコープ。前倒しは重い（量子化・グリッド割付・producible制約への丸め）。伎海判断。

### #9 消しゴムが行単位 — T0.5 UI改修/小機能
- 現状: 消しゴム(3)はドラッグ中も**開始行に固定**（`onMouseDown` で `drag.y` を固定し、`onMouseMove` は `drag.y` のみ消去）。複数行をまたいで自由に消せない。矩形の「一括消し」(`btn-rect-erase`)は在るが範囲選択(5)経由で導線が別。
- 方針メモ（解釈が2通り・要確認）:
  - (a)「行ロックを外して自由方向に消したい」→ eraser を現在行固定でなくポインタ追従に変更（小さめの実装）。
  - (b)「全行＝全消去（クリア）したい」→ 全クリアボタン追加。
  - **どちらの意図か池田さん/伎海に確認。** 両対応も可（自由消し＋全クリア）。

### #10 選択範囲の操作も左ツールバーへ — T0.5 UI改修
- 現状: 「範囲選択の操作」パネル（コピー/一括塗り/一括消し/連続ペースト）は右カラム。ツール本体は左。操作が左右に分離＝#11の分かりにくさの一因。
- 方針メモ: #1/#11と一体で、範囲選択の操作を左（ツール近傍）へ集約。ツール選択→即操作の動線に。

### #11 連続ペースト・一括塗りが使えない — バグ調査B（→第4章）
- 判定: **真バグではなく導線問題。** 機能は範囲選択(5)経由なら正常動作する。詳細と根拠は第4章。

---

## 3. 調査A — 「同色隣接ランの意図的な区切り」データモデル設計メモ

> 実装はしない（T0.5/T1）。以下は設計判断の材料。

### 3-1. 現状の確認（結論: 現モデルでは表現不可能）

- `doc.cells[y][x]` は **`null` か `colorId`（文字列）のスカラーのみ**（`js/trace-state.js` `emptyCells`/`paintRun`）。**ラン境界情報を一切持たない。**
- 「ラン（＝1本の渡り）」は保存されず、**読み取り時に行を走査して都度導出**される。導出は色が変わるか null で切れる、の1規則のみ。**同じ導出が3箇所に独立実装**されている:
  - `js/trace-validate.js` `extractRunsMulti(row)` … float/偶数ラン検出・目数集計
  - `js/trace-render.js` `rowRuns(row)` … 画面のバー描画
  - `js/trace-state.js` `runAt(cells,x,y)` … 選択（ラン）ツールの選択
- 帰結: **3目＋隣接4目（同色）は、7目1本と物理的に区別不能。** どこにも「x=4とx=5の間に切れ目がある（両方赤でも別ラン）」を書く場所が無い。→ フィードバック#3は**現モデルの構造的制約**であり確定。

### 3-2. なぜ重要か（float制約との接続・要確認）

- `floatMax=7`（変更禁止・producible制約）に対し、**同色で8目以上連続する領域は「1本の渡り」としては不可（>7）**。現状 8目の同色ランは float違反（赤・チャート不可）で弾かれる。
- 実際のこぎんでは幅7目超の同色面は「複数の渡りに分割し、間で糸を留める」ことで producible にする。**この分割＝区切りこそが#3の機能。** つまり渡り分けは見た目の話でなく、**幅広同色面を producible に表現する唯一の手段**の可能性が高い。
- → **これが無いと現ツールは「8目以上の同色面」を一律に非producible扱いして過剰に弾いている**恐れ。単なる装飾要望でなくモデルの穴。ただし「8目以上同色は本当に分割で作るのか／別構造なのか」は**池田さん/所長確認事項**（下記3-5）。

### 3-3. 候補比較（3方式）

**候補1: 区切りマーカーセル方式（cells内にセンチネル）**
- 案: cells に特殊値を入れて切れ目を表す。
- 問題: 切れ目は**セルの「間」（エッジ）**にあり、**セルの上には無い**。隣接する2ランの間にはマーカーを置くセルが存在しない（置けば1目潰れて刺し目が消える）。回避するにはセルを `null|colorId` から `{colorId, breakBefore:bool}` のオブジェクトへ格上げが必要＝**cells全体の型変更**（3導出・render・undoのclone・serializeすべてに波及）。
- 評価: 意味論的に不整合（切れ目はエッジ、セルはノード）。**却下。**

**候補2: 別レイヤの break集合方式（推奨）**
- 案: `cells` は現状のまま（`null|colorId`）。**別に「強制区切りエッジの集合」**を持つ。例: `doc.breaks = { "y": [x, ...] }`（「行yで x が左端の強制ラン開始＝x-1とxの間に切れ目」）。疎（切れ目がある所だけ）。
- cells不変 → 3つの導出関数に「色変化・null・**breakエッジ**でも切る」の1条件を足すだけ（各関数に breaksRow を渡す）。局所的。
- 影響:
  - **チャート出力**: 記号グリッドはセル単位（記号自体は不変）。ただし**紙のチャート上で同記号セル間の切れ目をどう見せるか**は別途レンダ追加が要る（細い仕切り線 or 慣習）。「紙面に切れ目表示が必要か」自体が要確認（3-5）。
  - **float検証**: `extractRunsMulti` が breakで分割 → 8目をx=4で切れば4+4で各≤7 → 違反解消。**#3と幅広面producible化の両方を一発で解決。**
  - **undo**: スナップショットに breaks を追加（`DocStore` の `snap`/`apply`＝現 `{grid,cells}` に `breaks` を足す）。小。
  - **保存形式**: doc に `breaks` フィールド追加＋serialize/deserialize対応。**後方互換**（無ければ空＝現在の挙動）。
  - **座標保守（主コスト）**: セル編集（paint/erase/move/resize/resizeGrid/paste）でエッジ座標がずれる/消える処理。T0.5は簡易規則で可（nullになったセル周りの breakは消す・moveRunでは基本引き継がない）。完全追従（moveRunで切れ目も一緒に動く）は追加コスト。
- 評価: **最小・局所・後方互換。cellsを汚さずに直交する疎オーバーレイで足す。推奨。**

**候補3: ランID方式（明示的セグメントを正本化）**
- 案: モデルを「セルの色グリッド」から「ランのリスト `[{y,start,len,colorId}]`」正本へ（or セルに `{colorId, runId}`）。隣接同色でも別オブジェクト＝自然に区別可。
- 影響: **モデルの全面変更。** cellsを読み書きする全関数（pen部分上書き・単セル消し・moveRun・resizeRun・copyRect・repeatPaste・render・validate・runAt・resizeGrid・serialize）をラン整合維持へ書き換え。塗りでのラン分割/結合、部分消しでのラン割り、重なり塗りのラン外科手術が必要。保存形式は別物（移行要）。
- 評価: 長期的には最も「正しい」がコスト・リスク最大。**T0.5では過剰。** T1で自動グリッド化・モドコ導入でモデルを触るなら、その時に一括検討する候補。

### 3-4. 推奨と T0.5コスト見積り

- **推奨: 候補2（break集合オーバーレイ）を T0.5。** 候補3はT1（モデル刷新と抱き合わせ）に先送り。候補1は却下。
- T0.5コスト概算（データ層＋検証/描画。UI操作系は#1/#10のUI改修に同梱）:
  - `trace-state.js`: doc に breaks（newDoc/cloneDoc/serialize/deserialize）、runAt に breaks対応、break追加/削除ヘルパ、DocStore snap/apply拡張、resizeGrid/eraseRange で break掃除 … **中**
  - `trace-validate.js`: `extractRunsMulti(row, breaksRow)` で分割 … **小**
  - `trace-render.js`: `rowRuns(row, breaksRow)` 分割＋（任意）切れ目の視覚表示 … **小〜中**
  - `trace-app.js`: 切れ目を打つ/消すUI（例: 選択モードでセル境界を Alt+クリック、or 専用ミニツール）… **中**（＝UI改修なので T0.5 UI一括に含める）
  - テスト: node/harness 拡張 … **小**
  - 合計 ≒ データ+検証+描画で 0.5〜1日相当。UI操作系は T0.5 レイアウト刷新に同梱。
- 既定挙動の設計判断（要UX決定）: 隣接同色塗りは**既定=従来通り統合**とし、ユーザーが**明示操作で切れ目を打つ**（＝「意図的に分けたい」に合致）。自動で切ると逆に統合できなくなる。

### 3-5. 池田さん確認事項（調査A関連）

- **「同じ色で渡りを分けるのはどういう時か」**（最重要）: (a)8目以上の同色面を producible にする分割か、(b)糸の留め/テンションか、(c)柄・構造上の意図か。→ 分けの意味づけで既定挙動と検証仕様が変わる。
- 幅7目超の同色面は、実際に「複数の渡り＋間で留める」で作るのか。作るなら floatMax=7 のままで良いが、**8目以上を一律違反にしている現仕様が過剰**になる（breakで解消する設計にすべきか）。
- **紙のチャートに intra-color の切れ目表示は必要か**、それとも切れ目は画面上の float分割/検証だけの内部概念で、印刷図は従来のセル記号のままで良いか。→ チャートレンダ追加要否が決まる。

---

## 4. 調査B — 「選択しても連続ペースト・一括塗りが使えない」再現調査

> 手法: `test/trace-harness.html` の合成 `setRect` API ではなく、**実UI導線**（ツールモード→矩形ドラッグ→実ボタンclick）を MouseEvent 合成で駆動。rectSel は必ず範囲選択ツールの実ドラッグ経由で生成した。専用プローブ `test/trace-ui-path-probe.html` を作成しヘッドレスChromeで実行。`window.alert` を記録して導線ゲートの発火を検出。

### 4-1. 判定: **(ii) 導線問題（真バグではない）**

機能自体は正常動作する。プローブ16チェック全pass（`ok:true`）。ソースは変更していない。

### 4-2. 根拠（プローブ結果）

- **PATH 1（範囲選択=tool5 の正規導線）**: 実ドラッグで矩形(5×4)を作り、実ボタンを押す。
  - 一括塗り(`btn-rect-fill`) → alertゼロ・20目塗り成功
  - コピー(`btn-copy`) → clip 5×4 生成成功
  - 連続ペースト(`btn-repeat`→フォーム表示→`rep-place`→キャンバスclick) → alertゼロ・20目配置成功
  - 一括消し(`btn-rect-erase`) → 消去成功
  - → **範囲選択(5)から入れば全機能が正常に効く。**
- **PATH 2（選択=tool4 ラン選択後）**: ランを1本選択してから一括塗り/連続ペーストを押す。
  - 一括塗り → `alert("先に範囲選択してください")` で弾かれ**無反応**（塗られない）
  - 連続ペースト → `alert("先にコピーしてください")` で弾かれ、フォームも開かない
  - → **これが「使えない」の正体。**
- **PATH 3**: 範囲選択(5)で矩形を作った後にツールをpenへ切替 → 一括塗りは依然成功（rectSelはツール切替で消えない）。

### 4-3. 原因（なぜ「使えない」と感じるか）

1. **「選択」という語のツールが2つあり紛らわしい。**
   - 選択（ラン）＝tool4: 単一ランを選ぶ。状態 `selection`。**矢印キーで移動/伸縮/再着色/削除**専用で、一括塗り/コピー等の**ボタンには一切繋がっていない。**
   - 範囲選択＝tool5: 矩形を作る。状態 `rectSel`。**一括塗り/コピー/一括消し/連続ペーストはこの `rectSel`（とコピーで作る `clip`）だけを見る。**
   - ユーザーが直感的な「選択」(tool4)を選んで一括塗りを押すと、rectSelが無く alert で弾かれる。**tool4とtool5の名前が近すぎる。**
2. **ボタンに状態が出ない（視覚アフォーダンス不足）。** 一括塗り/コピー/連続ペーストは rectSel/clip の有無で disabled/enabled を切り替えず、**常に押せるように見えて押すと alert するだけ。** 「矩形を選択済みか/ラン選択中か」の区別が画面に出ない。
3. **連続ペーストが多段の隠し導線。** 範囲選択(5)ドラッグ→コピー→連続ペースト…→フォーム入力→配置モードへ→キャンバスclick、と5手以上。各段が未達だと alert。途中で「壊れている」と感じて離脱しやすい（フォームは既定 hidden）。

### 4-4. 行き先（T0.5・修正はしない）

- 調査Bは真バグでないため**ソース修正なし**。以下を T0.5 UI改修に接続（#10/#11/#1と一体）:
  - 「選択（ラン）」と「範囲選択」の**名称・アイコンを明確に差別化**（例: ラン選択／矩形選択）。
  - 一括塗り/コピー/連続ペースト等を **rectSel/clip 無し時は disabled 表示**（押せないことを見せる）＋ヒント文言。
  - 範囲選択の操作パネルを**左（ツール近傍）へ集約**（#10）。ツール5選択→即操作が見える動線に。
  - 連続ペーストの多段を**フォーム常時表示 or ステップ表示**で「今どの段か」を可視化。
  - （任意）ラン選択(tool4)中に一括塗りを押したら、alertでなく「範囲選択(5)で矩形を選んでください」と**次の一手を提示**。

### 4-5. 再現物

- プローブ: `C:\dev\kogin-image-gen\test\trace-ui-path-probe.html`（新規・既存ソース不変）
- 実行: DEVGUIDE のヘッドレスChrome手順と同形式。`<div id="trace-harness-result">` の `RESULT:{...}` が `"ok":true`。
- baseline（改修なしを確認）: `node test/trace-state-test.cjs`＝65 passed / `node test/trace-validate-test.cjs`＝15 passed / `test/trace-harness.html`＝ok:true（いずれもソース無変更で維持）。

---

## 5. 池田さん確認事項まとめ（T0.5設計の入力）

1. **糸色・地布の実色**（#4・G-2実測）: 実こぎんの定番糸色・地布色・糸番号。→ `trace-config.js` の PALETTE/FABRICS を実データ化。
2. **同色で渡りを分ける状況**（#3・調査A）: どういう時に分けるか。8目以上の同色面は分割で作るのか。→ break機能の既定挙動・floatMax運用・チャート表示要否が決まる。
3. **消しゴムの意図**（#9）: 行ロック解除（自由消し）か、全クリアか、両方か。
4. **メニュー常用項目**（#1/#10）: 現場で最も触るツール・操作は何か（左に集約する対象の優先度）。

---

## 6. ラウンド2 — デザインツール型の操作性（#12・伎海FB 2026-07-23）

> T0.5改修（4commit・8a1cd4c〜4eea384）を実ブラウザで操作した伎海のフィードバック。
> 原文趣旨:「操作性がまだイマイチ。デザインツール／グラフィティ（お絵かき）ツールみたいな操作性にするといい」。
> 方向: Figma/Photoshop/ピクセルエディタの業界標準操作をこのエディタに踏襲し、他ツールで
> 体に入った操作がそのまま効く状態を目標にする。

### #12 デザインツール型の操作コア — T0.5.1（ラウンド2で実装）

実装する標準パターン:
1. **ズーム**: マウスホイール＝カーソル位置中心。下絵スケールは「下絵移動」ツール選択時のみに分離。100%/全体表示のリセット。
2. **パン**: Space+ドラッグ／マウス中ボタンドラッグで画面移動（どのツールでも効く）。
3. **ツールショートカット**: 文字キー（P/D/E/R/M/U/B）＋従来の1〜7。ボタン・ツールチップにキー表示。
4. **ドラッグ連続塗り**: ペンはドラッグで連続塗り（マス目スナップ・同一セル重複抑止・線分補間で隙間なし）。消しゴムも補間追従でスムーズに。
5. **スポイト**: Alt+クリックで塗られたマスの色をペン色に取得。
6. **カーソル連動**: ツールごとにカーソル形状変更＋ペン/消しゴムはホバー中セルをハイライト。
7. **Esc**: 進行中の操作（矩形選択・連続ペースト・切れ目・パン）をキャンセルして既定ツール（ペン）へ。

制約: vanilla JS・依存追加なし／`floatMax=7` 不変／break層・T0.5レイアウトを壊さない／既存テスト全green維持＋smokeに操作コアの新規チェック追加。

**実装結果（2026-07-23・dev）**: 上記1〜7を実装（trace-app.js 配線＋trace-state.js `lineCells`補間＋trace-render.js `hoverCell`＋editor.html/css）。カーソル中心ホイールズーム＋100%/全体表示、Space/中ボタンパン、文字キーP/D/E/R/M/U/B、ペン/消しゴムの線分補間連続塗り、Alt+クリックスポイト、ツール別カーソル＋ホバーハイライト、Esc→既定ツール。テスト全green（state 101・validate 27・harness 28・probe 20・editor-smoke 24→44）・push無し＝伎海の目視スモーク待ち。

**追加（2026-07-23・伎海FB「使い方ページもあると優しい」）**: 独立ページ `help.html`（非エンジニア向け・専門用語言い換え・①これは何か②基本の流れ③ツール表④便利操作⑤切れ目⑥うまくいかない時＝導線問題を先回り案内）を追加。editor.html ヘッダに「？ 使い方」ボタン（新規タブで help.html）。smokeに `b_help_button` チェック追加（→45）。

---

## 7. ラウンド3 — 下絵まわりの操作性（#13〜#16・伎海FB 2026-07-24）

> T0.5.1（ラウンド2）を実機スモークした伎海の下絵まわりフィードバック4点。トレース台の根幹＝
> 「マス目と下絵の重なり」がズームで1pxもずれないこと（#13）を最優先に据える。

### #13 ズームと下絵の一体化（最優先）— T0.5.1（R3で実装）
- 現状（バグ）: 下絵は `trace-render.js` で `ux=PAD+u.x`（生px）・`dw=u.w*u.scale`（zoom非依存）で描画される一方、グリッド／cells は `PAD+x*cw`（cw=zoom）で描画。**ズームするとグリッドだけ拡縮し下絵は据え置き＝マス目と下絵の相対位置がドリフトする。**
- 要望: ホイールズーム／表示倍率変更時、下絵もグリッドと同率・同中心で拡縮し、相対位置・相対スケールが常に固定。パンも一体。
- 方針: 下絵ジオメトリ（x/y/scale）を zoom非依存の world単位で保持し、描画時に係数 `uf=zoom/ZREF`（ZREF=zoom.init=10）で一律スケール。zoom=ZREF で従来描画と完全一致（後方互換）。centerUnderlay/fitUnderlay/下絵ドラッグも world単位に統一。パンは元から同一SVGのスクロール＝一体で動く。
- **実装結果（2026-07-24・dev）**: render に `uf` 係数＋app の view/center/fit/drag を world単位化。スモークで zoom 10→22 変更後、実描画 `<image>` px をセル単位換算した下絵ボックス(left/top/w/h)が不変を数値検証（`s13_left/top/w/h_fixed` 全pass）＝マス目と下絵の相対位置・スケールがズームで固定。



### #14 下絵の表示ON/OFFトグル — T0.5.1（R3で実装）
- 要望: ワンクリック＋ショートカット（H）で下絵の表示/非表示を切替。非表示でもデータ・位置・スケールは保持（再表示で元通り）。塗りの確認時に下絵を消して見る用途。
- **実装結果（2026-07-24・dev）**: `H` キー＋リボン「下絵」チェックで `underlayVisible` トグル（`view.hideUnderlay` で描画スキップ・`doc.underlay` は保持）。スモークで H→非表示で `<image>` 消失かつ `doc.underlay` 残存、再H で復帰を検証（`s14_*` 全pass）。

### #15 キャンバス直接ドラッグ&ドロップ — T0.5.1（R3で実装）
- 要望: 下絵画像をグリッド板（キャンバス）そのものにD&Dで投入。既存の右パネルのドロップゾーン/ファイル選択導線は併存。D&D中はキャンバスに「ここにドロップで下絵を読み込み」オーバーレイ表示。
- **実装結果（2026-07-24・dev）**: `#ws-canvas` に `wireCanvasDnd`（drop→`loadUnderlayFile`・右パネル導線は併存）。D&D中は `#canvas-dnd-hint` オーバーレイ表示。スモークで dragover→表示・dragleave→非表示・要素存在を検証（`s15_*` 全pass）。※ドロップ画像の実デコード（非同期）はヘッドレス未検証＝要実機。

### #16 マス目数の数値設定UI — T0.5.1（R3で実装）
- 現状: 縦横マス数の数値入力は右パネル（折りたたみ既定）の `grid-w`/`grid-h` のみ＝見つけにくい。
- 要望: 縦×横のマス数を数値直接入力で設定するUIを、見つけやすい上部リボンへ昇格（右パネルの既存入力は併存・相互同期）。既存塗りデータのリサイズ挙動は現行 `resizeGrid`（左上基準・落ちる刺し目は確認）を踏襲。
- **実装結果（2026-07-24・dev）**: 上部リボンに「マス目 横×縦＋適用」（`grid-w-top`/`grid-h-top`/`btn-grid-apply`）を追加＝`applyResize` 直結。`syncGridControls` で右パネル `grid-w/h` と双方向同期。スモークで 横30×縦20 適用→`grid.w/h` 反映かつ右パネル値同期を検証（`s16_*` 全pass）。

---

## 8. ラウンド4 — 全画面モード・下絵の精密操作（#17・#18・池田さんFB 2026-07-24）

> 池田さん（現場実務者・ハートスポット）の初フィードバック2点。R3までの構造（world単位下絵・break層・操作コア）を壊さず、
> vanilla JS・依存追加なし・floatMax=7不変で実装。

### #17 全画面モード（キャンバスだけの全画面表示で塗れる）— T0.5.3（R4で実装）
- 要望: 画面切り替えで上部リボン・左右パネルを全部隠し、キャンバスを最大化して塗れる全画面トグル。ボタン＋F キー、Esc で戻る。全画面中もツールのキーショートカット（P/E/R/M/U/B）・ズーム・パン・undo は全部効くこと。最小限のフローティングUI（現在ツール表示＋戻るボタン）は可。
- 方針: `body.focus-mode` クラス1枚でリボン・左右パネルを `display:none`・`.ws` を1カラム・キャンバスを最大化（CSSのみ・DOM構造とキャンバス寸法計算は不変）。トグルは canvas-toolbar のボタン＋`F` キー、`Esc` は全画面中まず全画面解除（ツールは保持）。ショートカット/ズーム/パン/undo は document/svg 直バインドなのでパネル非表示でも従来どおり有効。現在ツール表示（既存 `cur-tool-name`）＋戻るボタン（同トグルボタンが「全画面を終了」に変化）でフローティングUI要件を満たす。
- **実装結果（2026-07-24・dev）**: `focusMode` 状態＋`setFocusMode`/`toggleFocusMode`（body クラス切替・ボタン文言切替）。`F` キー・`btn-focus-toggle` でトグル、`Esc` で解除。editor.css に `body.focus-mode` レイアウト規則。スモークで F→ON・全画面中に `e` でeraser切替とホイールズームが効く・Esc→OFF・ボタントグルを検証（`s17_*` 全pass）＋CSS実機プローブで header/ribbon/左右パネルが `display:none`・`.ws` が1カラムを確認。

### #18 下絵の大きさ・位置の精密操作 — T0.5.3（R4で実装）
- 要望(a): スライダー（つまみ）で下絵のスケールを連続調整（下絵パネル内・数値表示付き）。(b): 矢印キー（上下左右）で下絵の位置を1px/マス単位で微移動（下絵移動ツール選択中・Shift併用で大きく移動）。既存「横N目フィット」（R3の#13 world単位ジオメトリ）と整合＝スライダーもキー移動もworld単位系で動くこと。
- 方針: (a) 下絵パネルに `underlay-scale` スライダー（値＝横N目相当・1〜200）。値→world単位スケール `scale=(N×ZREF)/u.w`（fitと同一式・ズーム非依存）。中心を保って拡縮＝矢印微調整と両立。数値表示 `uscale-val`＝「横 N 目」。wheel と同じ [0.1,10] でクランプ。(b) 下絵移動ツール中の矢印キーで `underlay.x/y` を world単位で加算（微＝1 world単位／Shift＝1マス＝ZREF）。どちらも `underlay` は undo対象外（既存のドラッグ/ホイールと同じ・cellsに影響なし）。
- **実装結果（2026-07-24・dev）**: (a) `underlay-scale` スライダー＋`setUnderlayWidthCells`/`setUnderlayScaleWorld`（中心保持拡縮）・`syncUnderlayControls`（読込/フィット/ホイール/中心合わせ/クリアで同期）。(b) onKey に下絵移動ツール中の矢印分岐＝`moveUnderlayBy`（world単位・Shiftで1マス）。スモークで スライダー→横60目相当へ拡縮かつ world中心が不変・ラベル更新（`s18a_*`）、矢印→+1world・Shift矢印→+ZREF・上→−1world・cells不変（`s18b_*`）を全pass。help.html④に全画面と下絵微調整の操作を追記。

**R4 検証記録（2026-07-24・dev）**: 純関数 state 101・validate 27／ヘッドレス trace-harness 28・trace-ui-path-probe 20・trace-editor-smoke **78**（R3の61＋R4の17＝s17×9・s18a×4・s18b×4）／新規 `test/trace-focus-css-probe.html` **13**（本物の `css/editor.css` を読み込み `body.focus-mode` で header/ribbon/左右パネルが `display:none`・`.ws` が1カラムになるのを `getComputedStyle` で実測）＝**全green**。既存の画像パイプライン（algo 12・producible 3320・pipeline violations 0）も維持。前セッションが着手した部分実装（未コミット・スモーク未追加・配線未完）を検証のうえ完成させた形。**push はしない**（main 検閲後）。

---

## 9. ラウンド5 — ブラウザ内保存メニュー（#19・伎海FB 2026-07-30）

> 「一度保存したものをメニューから参照できるようにしたい」。R4までの構造（world単位下絵・break層・操作コア・全画面）を壊さず、
> vanilla JS・依存追加なし・floatMax=7不変・データモデル不変で実装。保存の**置き場所を1つ増やすだけ**の変更。

### #19 保存済みデザインをエディタ内の一覧から開く — T0.5.4（R5で実装）
- 要望: (1) 名前を付けて保存（既存JSON保存と同じデータ構造を localStorage へ・名前の既定値＝日時）。(2) 保存済み一覧（名前・保存日時・64px程度の小サムネイル）から**クリックで読込**、未保存なら上書き確認。(3) 一覧からの削除・上書き保存。(4) 既存のJSONファイル保存/読込は残し、**役割分担を明記**（ファイル＝バックアップ・端末間共有／ブラウザ内＝この端末での続き）。(5) 「この端末のこのブラウザだけ・ブラウザのデータ削除で消える・大事な作品はJSONも併用」を UI と help.html に明記。(6) 容量対策（サムネは小さく・容量超過は握りつぶさずメッセージ表示）。
- 方針: 純ロジックを `js/trace-library.js` に分離（**storage 注入式**＝Node の fake storage でテスト可。DOM/canvasに触らない）。**索引と本体を別キーに分離**（索引 `kogin-trace-library-v1` にメタ＋サムネ／本体 `kogin-trace-doc-v1:<id>` に serialize文字列）＝1件保存で全件を書き直さない・一覧描画で本体を読まない。自動保存キー `kogin-trace-autosave-v1` は従来のまま併存（起動時の「続きを開く」は不変）。UIはモーダル1枚（`library-modal`）＋リボン「ファイル」に2ボタン＋役割注記。未保存判定は `dirty`（`scheduleAutosave` を全編集が通るのでそこで立てる／名前付き保存・読込・JSON保存で下ろす）。容量超過は `reason:'quota'` を返し、UI側でメニューを開いて定型文を表示（その場で古い保存を削除できる導線）。途中失敗は巻き戻し（新規は本体キー削除／上書きは旧本体を復元）＝孤児キー・不整合索引を残さない。
- **実装結果（2026-07-30・master）**: `js/trace-library.js`（新規・save/load/list/find/remove/usageBytes/available/isQuotaError・索引破損時は空扱いで死なない）＋ `trace-app.js` に配線（`saveToLibrary`/`promptSaveNew`/`openFromLibrary`/`overwriteLibrary`/`deleteFromLibrary`/`renderLibraryList`/`buildThumb`＝canvas 64px PNG・24KB超なら添付しない）。メニュー表示中はキャンバスのショートカットを停止し `Esc`／背景クリックで閉じる。editor.html にリボン2ボタン＋`file-role-note`＋モーダル、editor.css に `.modal-back`/`.lib-*`/`.rb-note`、help.html に⑥「保存のしかた（2種類あります）」＋トラブル2項目を追記。
- **既存機能は不変**: JSONで保存/JSON読込・自動保存・チャート出力・全画面・下絵まわり・cells/breaks/floatMax=7。

**R5 検証記録（2026-07-30・master）**（→ R6 は本ファイル末尾の第10章）: 純関数 state 101・validate 27・**library 45（新規）**／ヘッドレス trace-harness 28・trace-ui-path-probe 20・trace-focus-css-probe 13・**trace-editor-smoke 124（R4の78＋R5の46＝要素/役割注記/開閉/名前付き保存/既定名=日時/メタ/サムネdataURL/一覧DOM/開く/未保存確認とキャンセル/上書き/容量超過メッセージ/ショートカット停止/Esc/削除）**・**trace-library-css-probe 16（新規・モーダルの display・チャートより前面・サムネ64px角・注記の実効）**＝**全green**。既存の画像パイプライン（algo 12・producible 3320・pipeline violations 0・harness ok:true）も維持。実 `editor.html` をヘッドレスで直接読み込み、`canvas-status=準備完了`・`library-count=0件・約0KB`（＝実 localStorage で索引読み出しが動作）を確認。**push はしない**（伎海の実機目視後に main が push）。

---

## 10. ラウンド6 — 全画面中のツール切替パレット（#20・池田さんFB 2026-07-31）

> 現場（池田さん側）の要望。R5までの構造（world単位下絵・break層・操作コア・全画面・保存）を壊さず、
> vanilla JS・依存追加なし・floatMax=7不変・データモデル不変で実装。

### #20 全画面（フォーカス）モード中もツールを切り替えたい — T0.5.5（R6で実装）
- 課題: 全画面は `body.focus-mode` で `.ed-header`/`.ribbon`/`.ws-left`/`.ws-right` を `display:none` にする実装（#17）。
  そのため**左パネルのツールボタンが全部消える**。P/E などのショートカットを知っていれば切り替えられるが、
  実際に使うのは福祉事業所の利用者・職員＝**キーボードショートカット前提にはできない**（知らないと詰む）。
- 要望: 全画面中もツールを切り替えられるようにする。最低限ペンと消しゴム。
  詰め込みすぎない・現在ツールが見て分かる・キャンバス操作を邪魔しない・指で押せるタップ領域。

#### 却下された第1案: 左上フローティングパレット（2026-07-31・伎海裁定で破棄）
- 案: `main.ws-canvas`（`position:relative`）の中に `#focus-tools` を追加し `position:absolute` で左上に浮かせる。
  ペン(P)／消しゴム(E)／下絵移動(U) の3ボタン。表示切替は `body.focus-mode .focus-tools { display:flex }` のCSSのみ。
- **却下理由＝方眼（キャンバス）に被る。** 実測 74×194px の領域を新たに覆う。トレース台として板の面積を削るのは
  本末転倒という判断（伎海）。
- **この経緯を残す理由**: 同じ案を再提案しないため。「全画面中にUIを足す」と考えると左上フローティングが真っ先に出るが、
  この製品では**新たに覆う面積がゼロであること**が要件になる。

#### 採用案: `.canvas-toolbar` の「現在ツール名」表示を選択UIに置き換える
- 着眼: `.canvas-toolbar` は **`body.focus-mode` でも `display:flex` のまま残る**（実測・位置 [54,21]）。しかもそこには元から
  `<span>ツール: <b id="cur-tool-name">ペン</b></span>` ＝**現在ツール名の表示に場所を取っていた**。
  **その枠をそのまま選択UIにすれば、方眼を新たに覆う面積はゼロ**になり、全画面/通常で同じ場所・同じ操作になる。
- 実装: `<label class="tool-pick"><span class="tp-lbl">ツール</span><select id="tool-picker"></select></label>`。
  option は `buildToolSelect()` が `TOOLS` から生成し、`TOOL_KEYS` を逆引きして**「ペン［P］」の形でショートカットキーを併記**する。
- 切替は**既存 `setTool` をそのまま呼ぶ**（切替ロジックは新設しない）。逆に `setTool` の側で `$('tool-picker').value` を
  同期する＝**キーボードで切り替えても select の表示が追従**する（どの経路で切り替えても一致）。
- 旧 `cur-tool-name` を更新する行は**存在チェック付きで残してある**＝HTMLから消えても壊れない。
- 載せるツール: **7種すべて**。第1案が「ラン選択・矩形選択はあえて載せない」としていたのに対し、こちらは**載せた上で、
  全画面中に選ばれたらステータスバーで「Esc で全画面を抜けると左パネルに出ます」と案内する**。
  無効化はしない＝モードによって見た目を変えない（同じUIが同じように使える方を優先）。
- キャンバス操作を邪魔しない: select は `#trace-svg` の外＝描画ハンドラ（svg直バインドの `mousedown`/`wheel`）には
  **元から届かない**。加えて**選択後に `blur()`** ＝ P/E/F/Esc・Space（パン）などのキー操作が従来どおり効く
  （入力欄フォーカス中は `onKey` が無効化されるため、フォーカスを残すとショートカットが死ぬ）。
- タップ領域: `#tool-picker` は `min-height:40px` / `min-width:132px`（指で押せる大きさ・最長のツール名が入る幅）。
  select の共通ルール `width:100%` を `width:auto` で上書きしている。
- **実装結果（2026-07-31）**: `editor.html` の `.canvas-toolbar` 内 `cur-tool-name` を `label.tool-pick`＋`select#tool-picker` に置換／
  `css/editor.css` に `.tool-pick`/`.tp-lbl`/`#tool-picker`（幅・タップ領域・アクセント枠・focus outline）／
  `js/trace-app.js` に `buildToolSelect()` 追加＋`init` で呼び出し＋`setTool` 内での value 同期。
- 不変: `floatMax=7`・break層・cells/breaksモデル・world単位下絵ジオメトリ・全画面(#17)・保存(#19)・ショートカット全種。
  **`#focus-tools`/`.ft-btn` は存在しない**（第1案の撤去をテスト `s20_no_floating_palette` で検証している）。

**R6 テスト（2026-07-31）**: `test/gen-editor-smoke.cjs` に **#20 分 16チェック**を追加（全140）。
`test/trace-focus-css-probe.html` に select の表示・実効スタイル系 **5チェック**を追加（全22）。主な検証内容＝
select の存在／`.canvas-toolbar` の中にあること／**旧フローティングが撤去されていること（`#focus-tools` なし・`.ft-btn` 0個）**／
option が全7ツール分あること／選択値＝現在ツール／キーボード切替に select が追従（`key('u')` → `value === 'underlay'`）／
全画面中も select が生存し、そこからの切替で `TraceApp.getTool()` が変わること／Esc で通常モードに戻ること。
**⚠ テストは未実行（生成のみ確認・37154 bytes）。** `gen-editor-smoke.cjs` はテストHTMLの生成スクリプトであり
ランナーではないため、実行はブラウザで開く必要がある。**green の実測は取れていない**。
**push は伎海の指示（2026-07-31）で実施。**

---

## 11. ラウンド7 — 印刷（カラー・方眼の見やすさ）（#21・#22・池田さんFB 2026-08-06）

> 現場（池田さん・ハートスポット）から伎海経由の2点。R6までの構造（cells/breaks・world単位下絵・全画面・保存・ツールselect）を
> 壊さず、vanilla JS・依存追加なし・`floatMax=7` 不変・データモデル不変で実装。**変更は紙面（チャート）まわりのみ**で、
> エディタ本体の画面（`trace-render.js` のキャンバス描画・`editor.html`）は一切触っていない。

### #21 印刷すると糸の色が出ず白黒になる — T0.5.6（R7で実装）

- **真因（実物を出して確認）＝ `print-color-adjust` ではなく、チャートSVGがそもそも糸色を描いていなかった。**
  `js/trace-chart.js` の `buildChartSVG` は刺し目を **記号テキスト1色（`fill="#111"`）** でしか描かず、糸色は
  **凡例の小さなスウォッチだけ**にあった（＝設計どおりの「記号チャート」）。印刷経路（チャート出力…→印刷＝`window.print()`＋
  `@media print` で `#trace-chart-overlay` 以外を隠す）を headless Chrome で実際にPDF化した baseline でも、
  **凡例の `<rect fill="#B7282E">` 等はカラーで出ている**＝SVGの `fill` は落ちていない。つまり「色が落ちた」のではなく
  **最初から紙に色が無かった**。CSS/プリンタ設定は真因ではない（二次要因としては後述の2点を併せて手当て）。
- 対応:
  - `buildChartSVG(doc, vr, cfg, opts)` に `opts.color`（**既定 true**）を追加。カラー時は刺し目を
    **セル全面の糸色 `<rect class="chart-cell">`**（薄色が白紙に埋もれないよう `rgba(0,0,0,.18)` の淡い輪郭付き）で塗り、
    その上に**方眼線 → 記号**の順で重ねる（色の上でも方眼が数えられ、記号も残る）。
  - 記号は**糸色の輝度で黒/白に自動反転**（`symbolInk`＝lum<0.55 で白）。**白黒コピー・モノクロ機でも記号で色が判別できる**状態を維持。
    カラー時は記号を小さめ（`CELL*0.62`）にして、マスの色が記号に潰されないようにした。
  - 印刷オーバーレイのバーに **「カラーで印刷（糸の色をそのまま出す）」チェック（既定ON・`#tco-color`）** を追加。
    外すと従来どおりの記号のみ白黒チャート。PNG保存も同じ設定で出る（`exportPNG(doc,vr,cfg,opts)`）。
  - 二次要因の手当て（CSS `@media print`）: ①`print-color-adjust: exact`（`-webkit-`／`color-adjust` 併記）を
    オーバーレイ／シート／SVG配下に明示＝economy で色を落とすブラウザ・ドライバ対策。②`html, body { background:#fff }`＝
    **画面用のダーク背景が紙の下部に黒帯で刷られていた**（背景グラフィックONの環境）のを解消。
    ③オーバーレイ下に「プリンタ側が白黒/グレースケールだと色は出ません」の注記（`.tco-tip`・印刷時は非表示）。

### #22 印刷時のマス目が細かく出すぎる — T0.5.6（R7で実装）

- 現状: チャートの方眼は **毎目 `#c8c8c8` 0.6px ／ 5目ごと `#333` 1.2px の2階層**のみ。A4に縮めると細線が密集し、
  区切りの手がかりが弱い（80×100目だと約0.6倍に縮小＝細線が潰れて全面が網目に見える）。
- 対応: こぎん方眼の通例に合わせ **3階層**へ。`lineTier()` で `1目=cg-thin (#d9d9d9 / 0.4)`・
  `5目=cg-5 (#8c8c8c / 0.9)`・**`10目と外枠=cg-10 (#1f1f1f / 1.6)`**。薄い順に描いて太線を上に重ねる。
- **@media print 側で紙用に微調整**（`line.cg-thin` 0.35/#cfcfcf・`cg-5` 1/#6f6f6f・`cg-10` 1.8/#141414）。
  CSSルールは SVG の presentation attribute を上書きできるため、**属性値＝画面プレビュー/PNGの既定・CSS＝紙の最終調整**という
  役割分担にした（PNG書き出しは Blob→Image 経由でページCSSが効かないので、階層自体は属性側に持たせる必要がある＝
  「@media print だけで完結」にしなかった理由）。

**R7 検証記録（2026-08-06・dev）**:
- 新規 `test/trace-print-probe.html`（実 `css/editor.css` ＋実 `trace-chart.js` を読み、41×41の多色柄を作って
  `TraceChart.open` を実行。`?color=0` で白黒モード・`?w=80&h=100` で名刺入れ相当）。headless Chrome で
  **`--print-to-pdf` の実PDF**と `--dump-dom` の自己検証を両方取得。
  - カラー: `RESULT:{"ok":true,...,"colorCells":854,"symbols":854,"cellFillHexes":["#27407A","#6E9E4F","#7FAFD4","#B7282E","#E3A93D","#F2EEE3"],"thin":64,"g5":8,"g10":12,"strokeThin":0.4,"stroke5":0.9,"stroke10":1.6}`
    ＝**使用6色すべてがグリッド内のセル塗りとして紙に出ている**／方眼3階層が本数・太さともに成立。
  - 白黒（`?color=0`＝チェックOFF相当・change ハンドラ経由で再生成）: `RESULT:{"ok":true,...,"colorCells":0,"symbols":854}`。
  - 実PDF目視: baseline（改修前）は**記号が全部黒＋ページ下部に黒帯**／改修後は**糸色で刷られ黒帯も消滅**、
    10目の太線・5目の中太・毎目の薄線の階層が紙面で判別可能（80×100の実寸でも1枚に収まり階層が効く）。
- 既存テスト全green（回帰なし）: 純関数 state 101・validate 27・library 45／headless harness ok:true・
  ui-path-probe ok:true・editor-smoke ok:true（140）・focus-css-probe ok:true（22）・library-css-probe ok:true（16）／
  画像パイプライン algo 12・producible 3320・pipeline violations 0。`test/trace-editor-smoke.html` は再生成しても
  37154 bytes で不変＝`editor.html` 無変更を機械確認。エディタ画面のスクリーンショットも改修前と同一構成であることを目視確認。
- **未検証（要実機）**: 実プリンタでの発色（インク・用紙・ドライバ依存）／PNG保存ボタンの実ダウンロード（headlessでは
  ダウンロード確認不可・SVG生成側は同じ `opts` を通すことをコードで担保）。
- **残課題（今回は対象外・要判断）**: 80×100目をA4 1枚に収めると1マス≒2.3mm で、刺しながら数えるには小さい。
  「大きめに刷る（複数ページ分割印刷）」は別要望として保留（池田さんに要否確認）。

**push はしない**（伎海の実機目視後に main が判断）。
